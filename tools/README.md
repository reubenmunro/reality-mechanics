# Tools

Operational helpers that are not live runtime source.

- `deploy/` keeps command-line deployment helpers and batch scripts.

Root-level `.command` files are left at the workspace root when moving them would
change their double-click behaviour or path assumptions.
